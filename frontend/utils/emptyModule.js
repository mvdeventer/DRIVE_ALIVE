/**
 * Empty module stub for browser-only packages (jspdf, exceljs).
 * Metro resolves these on native platforms to prevent stack overflow errors.
 * The actual libraries are only used on web via dynamic import().
 */
module.exports = {};
